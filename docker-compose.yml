services:
  app:
    build: .
    expose:
      - "3000"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
      - DATA_DIR=/app/data
    volumes:
      - lrn_data:/app/data
    restart: unless-stopped

volumes:
  lrn_data:
